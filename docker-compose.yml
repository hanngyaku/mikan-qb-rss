services:
  app:
    build: .
    environment:
      DB_PATH: /app/data/app.db
      LOG_PATH: /app/data/app.log
      LISTEN_ADDR: :8081
    ports:
      - "${WEB_PORT:-8080}:8082"
    volumes:
      - app-data:/app/data
    restart: unless-stopped
volumes:
  app-data:
